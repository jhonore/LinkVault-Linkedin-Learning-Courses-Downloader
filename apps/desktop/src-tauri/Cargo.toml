[package]
name = "linkvault"
version = "0.2.26"
description = "LinkedVault local archive workspace"
authors = ["LinkedVault"]
license = "MIT"
repository = "https://github.com/Howard-Starfield/LinkVault-Linkedin-Learning-Courses-Downloader"
edition = "2021"
rust-version = "1.77.2"
default-run = "linkvault"

[lib]
name = "linkvault_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[features]
crop-baseline = []
durability-baseline = []
youtube-process-test = []

[[bin]]
name = "linkvault"
path = "src/main.rs"

[[bin]]
name = "youtube_process_fixture"
path = "tests/fixtures/youtube_process_fixture.rs"
required-features = ["youtube-process-test"]

[[test]]
name = "youtube_managed_process_windows"
path = "tests/youtube_managed_process_windows.rs"
required-features = ["youtube-process-test"]

[[example]]
name = "newspaper_clipping_crop_baseline"
path = "examples/newspaper_clipping_crop_baseline.rs"
required-features = ["crop-baseline"]

[[example]]
name = "newspaper_clipping_note_durability_baseline"
path = "examples/newspaper_clipping_note_durability_baseline.rs"
required-features = ["durability-baseline"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
aes-gcm = "0.10"
base64 = "0.22"
bytes = "1"
chrono = { version = "0.4", features = ["serde"] }
image = { version = "0.24.9", default-features = false, features = ["jpeg", "png", "webp"] }
mime = "0.3"
regex = "1"
rusqlite = { version = "0.32", features = ["backup", "bundled"] }
reqwest = { version = "0.12", default-features = false, features = ["blocking", "cookies", "json", "rustls-tls-native-roots"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
ed25519-dalek = "2"
semver = "1"
tauri = { version = "2", features = ["tray-icon"] }
tauri-plugin-dialog = "2"
tauri-plugin-opener = "2"
tauri-plugin-single-instance = "2"
tempfile = "3"
thiserror = "2"
tokio = { version = "1", features = ["fs", "rt-multi-thread", "macros", "time"] }
url = "2"
unicode-normalization = "0.1.25"
webp = "0.3"
zip = { version = "2", default-features = false, features = ["deflate"] }
tauri-plugin-updater = "2"

# Windows-only. Previously declared unconditionally, which pulled the Win32
# binding crate into every non-Windows build even though all of its uses are
# `#[cfg(windows)]`.
[target.'cfg(windows)'.dependencies]
windows-sys = { version = "0.61", features = [
  "Win32_Foundation",
  "Win32_Security",
  "Win32_Security_Cryptography",
  "Win32_Storage_FileSystem",
  "Win32_System_IO",
  "Win32_System_JobObjects",
  "Win32_System_Pipes",
  "Win32_System_ProcessStatus",
  "Win32_System_SystemInformation",
  "Win32_System_Threading",
] }

# macOS-only. `security-framework` is already compiled on this target as a
# transitive dependency of reqwest (via rustls-native-certs), so declaring it
# directly adds no build weight. It backs the keychain half of `app::dpapi`.
[target.'cfg(target_os = "macos")'.dependencies]
security-framework = "3"
zeroize = "1"

[dev-dependencies]
wiremock = "0.6"

[profile.release]
codegen-units = 1
lto = "thin"
opt-level = "z"
panic = "unwind"
strip = true
